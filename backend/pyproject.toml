[tool.ruff]
line-length = 115
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]
ignore = ["B008"]  # Depends()/Query()/Body() em defaults é o idioma padrão do FastAPI

[tool.mypy]
ignore_missing_imports = true
warn_unused_ignores = true
exclude = ["tests/"]
